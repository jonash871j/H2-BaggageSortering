<UserControl x:Class="WpfApp.UserControls.AirportConsolesControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <GroupBox Grid.Row="0" Header="Sorting Machine Info">
            <local:ConsoleControl x:Name="CCon_SortingMachineInfo" Margin="4, 4"/>
        </GroupBox>
        <GridSplitter Grid.Row="1" HorizontalAlignment="Stretch"/>
        <GroupBox Grid.Row="2" Header="General Info" >
            <local:ConsoleControl x:Name="CCon_GeneralInfo" Margin="4, 4"/>
        </GroupBox>
    </Grid>
</UserControl>
